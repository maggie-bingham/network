<p id="notice"><%= notice %></p>

<h1>Your Events</h1>
  <div>

    <% @events.map do |event| %>
    <ul>
      <li><strong><%= link_to event.group_name, event_path(event["id"])%></strong></li>
      <li><%= strip_tags(event.description) %></li>
      <li><%= event.date %></li>
    </ul>
      <%= link_to 'Attend' %>
      <% end %>
  </div>


<%= link_to 'New Event', new_event_path %>
