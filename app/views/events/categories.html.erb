<p id="notice"><%= notice %></p>

<h1>Please Choose A Category</h1>
  <div>

    <%= @events.each do |group| %>
      <ul>
        <strong><%=  group.categories %></strong>
      </ul>
    <%end%>
  </div>
